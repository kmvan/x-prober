{
  // You can disable listed features: serverStatus, networkStats, ping, serverInfo, phpInfo, phpInfoDetail, phpDisabledFunctions, phpDisabledClasses, phpExtensions, phpExtensionsLoaded, database, myServerBenchmark, myInfo
  // "disabled": ["myServerBenchmark"]
  //
  // TODO: Nodes
  // You can set nodes for X-Prober
  // "nodes": [
  // ["node-1", "http://localhost:8000"],
  // ["node-2", "http://localhost:8000"],
  // ["node-3", "http://localhost/prober/.tmp"]
  // ]
}
